{% extends "tools/base.html" %}
{% load staticfiles %}
{% block main %}
<div>
    <aside class="container">
        <ul>
			{% for container in containers %}
            <li><a href="{{ container.dest }}"><img width="65%" src="{{ container.image }}"><br><span>{{ container.name }}</span></img></a></li>
            {% endfor %}
        </ul>
    </aside>
</div>
<main>
    <section>
        {% for indexcontent in content %}
        <article  class="content">
            {{ indexcontent.content }}
        </article>
        {% endfor %}
    </section>
</main>
{% endblock main %}
