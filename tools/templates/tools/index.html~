{% extends "tools/base.html" %}
{% load staticfiles %}
{% block main %}
<div>
    <aside class="container">
        <ul>
			{% for container in containers %}
            <a href="{{ container.dest }}"><li style="background-image: url(/upload/gazm.jpg);" class="image-wrap container-image"><span>{{ container.name }}</span></li></a>
            {% endfor %}
        </ul>
    </aside>
</div>
<main>
    <section>
        {% for indexcontent in indexcontents %}
        <article  class="content">
            {{ indexcontent.content }}
        </article>
        {% endfor %}
    </section>
</main>
{% endblock main %}
