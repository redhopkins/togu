{% extends "tools/base.html" %}
{% load staticfiles %}
{% block map_js %}
{% endblock map_js %}
{% block main %}
<section>
    <article class="content">
        <h1>Контакты</h1>
            <table class="contacts">
                <tr>
                {% for contact in contacts %}
                    <td><img src="{% static 'tools/img/phone.png' %}"></td>
                    <td>{{ contact.phone }}</td>
                <tr>
                    <td><img src="{% static 'tools/img/email.png' %}"></td>
                    <td>{{ contact.email }}</td>
                </tr>
                <tr>
                    <td><img src="{% static 'tools/img/address.png' %}"></td>
                    <td>{{ contact.address }}<br>(станция метро "{{ contact.metro }}")</td>
                {% endfor %}
                </tr>
            </table>
        <div id="map" style="width: 100%; height: 400px"></div>
    </article>
</section>
{% endblock main %}
