{% extends "tools/base.html" %}
{% load staticfiles %}
{% block main %}
<section>
            <div class="block">
                <ul>
					{% for toollistitem in toollistitems %}
                    <li class="box shadow">{{ toollistitem.name }}</li>
                    {% endfor %}
                </ul>
            </div>
            <div class="container">
                <!--[if lte IE 8]>
                <noscript>
                <style>
                #information li { overflow: visible; position: relative; margin: 0 auto; margin-bottom: 25px; background: #fff; width: 600px; padding: 30px; height: auto; list-style: none; }	
                #information li div a.close { position: relative; background: transparent; padding: 0; color: #0090e2; font-size: 12px; font-weight: normal; left: 0; top: 0; }	
                iframe, .backface { display: none; }
                </style>
                </noscript>
                <![endif]-->
                <ul id="grid">
					{% for tool in tools %}
                    <li class="open_popup" rel="popup1">
                        <div class="details" >
                            <h3>{{ tool.tool_name }}</h3>
                            <p>{{ tool.tool_price }} руб</p>
                        </div>
                        <a class="more" href="#info1"><img src="/{{ tool.tool_image }}"></a>
                    </li>
                    {% endfor %}
                    
                </ul>
            </div>
            <div class="overlay">
                <div class="backdrop">
                </div>
                <div class="popup" id="popup1">
                    <span class="close">X</span>
                    <img src="img/sci.jpg">
                    
                    <div class="block popup_form">
                        <ul>
                            <li class="box shadow">Пинцеты</li>
                            <li><span class="border">Param 2135</span></li>
                            <li><span class="border">Param 20</span></li>
                            <li><span class="border">Param 0152</span></li>
                        </ul>
                    </div>
                    </div>
                </div>
                </div>
            </div>
        </section>
{% endblock main %}
